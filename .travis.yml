language: python
python:
        - '3.7'
install:
        - 'pip install pipenv'
        - 'pipenv install'
env: DJANGO_SETTINGS_MODULE=tierslieux.settings.travis

services:
        - postgresql
script:
        - python manage.py test